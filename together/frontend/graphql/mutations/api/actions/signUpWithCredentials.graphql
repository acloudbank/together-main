mutation signUpWithCredentials(
    $email: String!
    $password: String!
    $accountType: String!
    $username: String!
  ){
    signUpWithCredentials(
      email: $email
      password: $password
      accountType: $accountType
      username: $username
    ) {
      sessionId
    }
  }