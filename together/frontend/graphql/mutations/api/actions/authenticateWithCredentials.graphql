mutation authenticateWithCredentials(
    $email: String = ""
    $password: String = ""
  ) {
    authenticationResult: authenticateWithCredentials(
      email: $email
      password: $password
    ) {
      sessionId
    }
  }