table:
  name: userSource
  schema: public
is_enum: true
array_relationships:
  - name: users
    using:
      foreign_key_constraint_on:
        column: source
        table:
          name: users
          schema: public
