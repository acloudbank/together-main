table:
  name: issueFieldViewScale
  schema: public
is_enum: true
array_relationships:
  - name: issues
    using:
      foreign_key_constraint_on:
        column: fieldViewScale
        table:
          name: issues
          schema: public
